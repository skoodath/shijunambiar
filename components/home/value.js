import styles from '../../styles/landing.module.scss';

const Value = () => {
    return (
        <div className={styles.value}>
          <p className={styles.value_para}>I create static websites or dynamic websites that make use of public APIs.</p>
        </div>
    )
}

export default Value;